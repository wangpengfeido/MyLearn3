# 函数定义时，参数顺序必须为：必选参数-默认参数-可变参数-关键字参数/命名关键字参数
# 调用时将参数按顺序传入
def f1(a, b=1, *c, **d):
    print(a, b, c, d)


# 这里要注意的是，2被赋给b
f1(1, *(2, 3), m=4)


def f2(a, b=1, *c, d):
    print(a, b, c, d)


f2(1, 2, 3, d=4)
