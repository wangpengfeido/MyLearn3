int main(int argc, char const *argv[])
{
  // 运算符分为一元运算符和二元运算符。

  // 组合运算符和运算对象：
  // 运算符有优先级、结合律以及运算对象的求值顺序。

  // 运算对象转换:
  // 运算对象常常由一种类型转换成另外一种类型。

  // 重载运算符：
  // 运算符作用于类类型的运算对象时，用户可以自行定义其含义。

  // 左值和右值：
  // 当一个对象被用作右值的时候，用的是对象的值（内容）；当对象被用作左值的时候，用的是对象的身份（在内存中的位置）。
  // 不同的运算符对运算对象的要求各不相同。返回值也有差异。

  return 0;
}
